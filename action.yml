name: 'Docker YML for Github Action '
description: 'Running the Dockerfile for Eze testing'
author: 'riversafe'

inputs:
  upload_method: 
    description: The method chosen by the user to handle Eze results (1. sarif or 2. markdown)
    required: true
    default: "2"
outputs:
  sarif_file:
    description: 'JSON content of the file generated by eze'
runs:
  using: 'docker'
  image: 'Dockerfile'
  # args:
  # - ${{ inputs.upload-method }}